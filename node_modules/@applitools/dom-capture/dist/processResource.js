
function() {
  var processResource = (function () {
  'use strict';

  // const fetchUrl = require('./fetchUrl');
  // const extractResourcesFromStyleSheet = require('./extractResourcesFromStyleSheet');
  // const findStyleSheetByUrl = require('./findStyleSheetByUrl');

  // const absolutizeUrl = require('../shared/absolutizeUrl');

  function processResource$1(url, doc, processResourceCache) {
    // return fetchUrl(url).then(({url, type, value}) => {
    //   if (/text\/css/.test(type)) {
    //     const styleSheet = findStyleSheetByUrl(url, doc);
    //     const resourceUrls = extractResourcesFromStyleSheet(styleSheet).map(resourceUrl =>
    //       absolutizeUrl(resourceUrl, url),
    //     );
    //     return getResourceUrlsAndBlobs(resourceUrls, url, doc, processResourceCache);
    //   } else {
    //     return {blobs: {[url]: {type, value}}};
    //   }
    // });
  }

  var processResource_1 = processResource$1;

  return processResource_1;

}());

  return processResource.apply(this, arguments);
}